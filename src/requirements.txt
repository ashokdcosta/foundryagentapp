# Requirements for Foundry Agent App
# This file is at src/ level so it's found when azure.yaml points to ./src as project root.
# Oryx build system will install these dependencies during deployment.

# FastAPI and server
fastapi==0.104.1
uvicorn==0.24.0
gunicorn==21.2.0

# Environment and configuration
python-dotenv==1.0.0

# Azure SDK
azure-ai-projects>=1.0.0,<2.0.0
azure-ai-agents>=1.1.0,<2.0.0
azure-identity>=1.16.0

# Testing
pytest==7.4.0
httpx==0.25.0
